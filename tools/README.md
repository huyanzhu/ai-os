# tools（v1 迁移 · 待接线验证）

> 请阅读 `README.md` 并按照规范执行；正在执行或执行过不管。

- `wrapup_sync.py` / `experience_push.py` / `task_card.py`：从 v1 复制/新建，路径按自身位置计算（可移植，已核）。
- `observe_extract.py` / `task_start.py`：V2 新增（观察提取 / 统一开工发现）。
- ✅ 里程碑 2 已完成：experience_push / wrapup_sync / task_card 已实跑（SUCCESS_LOG Entry #1–#22 实证）；另有 observe_extract（观察提取）、task_start（统一开工发现）。
