# AI-OS 基础设施体检报告（2026-08-23）

> 执行：Health Check（`maintenance-skills/health-check/SKILL.md`）首次真实运行。
> 纪律：只读扫描，未修改任何基础设施文件；发现只报告，交 Human 裁决（修 / 不修 / 以后修 / 忽略）。
> 范围：`D:\AI-os` 交付本体（不含 `D:\AI_v2`、不含 `~/.workbuddy` 等外部路径内容）。
> 方法：目录盘点 + git 状态 + 前端信息提取（frontmatter 四键）全量扫描 + 工具静态审查（experience_push / task_start / task_card）+ 账本逐条核对 + MAP 对照磁盘。

## 严重度口径

- **High**：直接影响检索/工作记忆/安全网等承重能力，或内容已损坏不可读。
- **Medium**：索引/文档/状态与磁盘或账本明显不一致，维护成本随时间累积。
- **Low**：过期声明、引用错位、顺序/编号瑕疵，不影响当前运行。
- **Info**：诚实声明（含"未发现问题"与设计决定提示）。

## Findings

| Finding | Severity | Infrastructure | Suggested Maintenance Skill | Evidence |
|---|---|---|---|---|
| `domains/index.md` 统计与实际磁盘不符：ai-os/patterns 表记 20（实际 23）、ai-os/experiences 表记 8（实际 10）、总量记"81+"（实际 92 篇非 README md）；顶层 `domains/experiences/` 4 篇元规则不在"4 域 × 3 类"表内 | Medium | domains 索引 | knowledge-maintenance | `domains/index.md` 表 vs 磁盘计数（ai-os 23/10/5、backend 40、frontend 5+1、product 4、顶层 4 = 92） |
| `domains/index.md` 待办过期："frontend 域覆盖为零"（实际 5 pattern + 1 guideline）；"3 篇 08-02 未提交 experiences 待补提交"（10 篇全部被 git 跟踪） | Low | domains 索引 | knowledge-maintenance | `domains/index.md` 待办节 vs `domains/frontend/**`、`git ls-files domains/ai-os/experiences`（10/10 已跟踪） |
| 2 篇 pattern 正文/元数据为已固化的 GBK 错位乱码（UTF-8 可解码但内容不可读）：`definition_preservation_check.md`（frontmatter title/condition/action + 正文全乱码，且 trigger 末项与 condition 粘连同一行）、`tomcat_port_conflict.md`（frontmatter 正常、正文全乱码） | High | domains 内容质量 | knowledge-maintenance | 文件原文："璺ㄩ樁娈佃繃娓℃椂鎵ц Definition Preservation Check"、"鐙珛 Tomcat 閮ㄧ讲绔彛鍐茬獊"；违反 `knowledge_frontmatter_rule` 的乱码即损坏判据 |
| FC-006 Definition Drift 簇三篇重叠碎片：`definition_drift_prevention` 与 `definition_preservation_phase_exit` 的 trigger/action 高度同质（Phase 过渡/标准升级 → 旧条件迁移检查），`definition_preservation_check` 为乱码篇 | Medium | domains 碎片/重复 | knowledge-maintenance（查重/归并判定；D-013"同源≠同原则"先例适用于判断） | 三文件 frontmatter：`knowledge_cluster: FC-006 Definition Drift`，trigger/action 逐条对比 |
| `workspaces/` 内 5 份完整 domains 过期副本：a16_condA、a16_condB、a16lite_A、a16lite_B、book_exp 各含 91 篇 md，全部缺失 2026-08-23 新增（debug_protocol / sqlite_backup_include_wal / startup_dependencies_injectable） | Medium | domains 碎片/重复 | knowledge-maintenance + Human 裁决（实验快照保留或归档） | 各副本 md 计数=91 vs 根 92；`Test-Path` 三个新 pattern 均为 False |
| 3 篇知识文件缺 `keywords` 键（`unverified_structure_reference.md`、`silent_false_green.md`、`stderr_error_channel.md`），frontmatter 四键规范未满足，检索权重（keywords=3.0）无法命中 | Medium | domains frontmatter 合规 | knowledge-maintenance | 全量 frontmatter 扫描：3/92 非 README md 文件 kw=False |
| 顶层 `domains/experiences/` 4 篇元规则（命名/失败观察/入库审查/frontmatter 规范）不在 `experience_push` 检索源内——`collect_files()` 只扫 `domains/<domain>/experiences/`，顶层目录被当作 domain 名，4 篇永不被推送命中；且 `domains/experiences/README.md`、`domains/patterns/README.md` 均写"v2 阶段暂空"已失真 | High | domains 检索盲区/文档 | knowledge-maintenance + retrieval 裁决（迁目录或改扫描） | `tools/experience_push.py` `collect_files()` vs `domains/experiences/*.md`（4 篇 tracked）、README 原文 |
| `tools/task_start.py` 能力名解析取错列：`capabilities()` 用 `cells[0]` 作 name（实际是行号"1"），正确名称在 `cells[1]`——统一开工发现打印"[能力] 1（trigger …）"而非能力名 | Medium | capabilities 发现 | 无现成 maintenance Skill（D-014 未建 capability-maintenance）→ Human 裁决 | `tools/task_start.py` `capabilities()` + `capabilities/INDEX.md` 表头（# / Capability / Source / Trigger / Activation / Status / Decision） |
| `capabilities/INDEX.md` 的 Decision 引用缺 D-010：INDEX 行记 D-006/D-007/D-011，单位文件 frontmatter 记 D-006/D-007/D-010/D-011（D-010 为 Facet 模型冻结，正文多处引用） | Low | capabilities 索引 | 同上（capability-maintenance 未建） | INDEX.md 首行 vs `capabilities/senior-engineer.md` frontmatter `decision_ref` |
| senior-engineer 状态未同步 D-012：INDEX 与单位文件 status 仍为"Consumed: 1st sample / Influence 40"，未反映 D-012 的 **Fused + Validated-1st-sample / influence 80 / Entry #20** | Low | capabilities 状态 | 同上 | INDEX.md / `senior-engineer.md` status vs `evolution/decisions.md` D-012 + SUCCESS_LOG Entry #20 |
| capabilities 未发现覆盖重叠 / 触发冲突：当前唯一单位 senior-engineer 与 code-review / security-check pattern 的互补关系已在单位文件内显式声明（机制在 domains、深度纪律在 Facet） | Info | capabilities | — | 无冲突证据；`senior-engineer.md` "与既有对象的关系"节 |
| TASK-20260820-002 卡 ID 在 active 与 archive 同时存在（active="V2 3–6 观察记录"，archive="task_card 工具自测"未填模板卡）——工作记忆 ID 唯一性被破坏，`_next_id` 双目录防复用被手工建卡绕过 | High | task_cards 索引完整性 | 无现成 maintenance Skill（D-014 未建 task-memory-maintenance）→ Human 裁决 | `task_cards/active/TASK-20260820-002.md` 与 `task_cards/archive/TASK-20260820-002.md` 同 ID 两卡 |
| 10 张交付完成卡待验收归档积压（LinkVault 6 张 20260821-010 + 20260822-001~005、DevJournal 2 张 20260822-007/008、ai-consultant 2 张 20260823-006/007），卡内状态均为"交付完成，待用户验收后归档"，与 SUCCESS_LOG 成功 Entry 对应 | Medium | task_cards 归档流程 | 同上（Human 验收后批量归档） | 卡片 State/Next Action vs SUCCESS_LOG Entry #7–#13/#20/#21 |
| 卡片状态与账本不同步：TASK-20260823-006/007 的 Current Step 写"wrapup_sync 与 SUCCESS_LOG Entry 待执行"，但 SUCCESS_LOG Entry #20/#21（同 TASK ID）已存在——卡片未在账本写回后更新 | Medium | task_cards 状态 | 同上 | 两卡 Current Step vs `AI-OS_SUCCESS_LOG.md` Entry #20/#21 |
| 项目归属不一致：DevJournal 的任务卡（007/008）平铺在 `active/` 根，而其 PROJECT.md 在 `active/DevJournal/` 项目文件夹内；ai-consultant 无项目文件夹/PROJECT.md（README 约定"项目文件夹内 PROJECT.md"，MAP 列为真实项目） | Medium | task_cards 项目归属 | 同上 | `task_cards/active` 目录树 vs `task_cards/README.md` 结构约定 |
| active 卡 TASK-20260820-001 引用已删除结构：Resume Point / Event Log 引用 `star_map/94_star_map.md`，git status 显示 star_map/ 两文件已删除（未提交 D） | Low | task_cards 死引用 | 同上 | 卡片行 vs `git status`（`D star_map/94_star_map.md`、`D star_map/94_star_map_audit_backup_20260820.md`） |
| `experience_push` 检索源死引用/盲区：SOURCES 含 `workflows/` 与 `env/`（均不存在，静默跳过）；task_card 源只扫 `task_cards/` 顶层（仅 README/模板可命中，active/archive 卡不入索引）；`_goal_intent()` 读已删除的 CURRENT_GOAL.md（恒空） | Medium | 检索 | retrieval-maintenance（D-014 未建；A18 reshape R1/R2/R3 已被 D-008 暂缓，属已知待 Human 裁决） | `tools/experience_push.py` SOURCES/GOAL_FILE + `task_cards/README.md` + `evolution/decisions.md` D-001/D-008 |
| SUCCESS_LOG 账本分块检索产生重复候选：`collect_ledger_chunks()` 按 bullet 分块且共用 Entry header，同一 Entry 多 bullet 命中时输出重复行——Entry #16 的 matched 已实录"Entry #15（重复项）" | Low | 检索去重 | retrieval-maintenance | `AI-OS_SUCCESS_LOG.md` Entry #16 matched vs `experience_push.py` `collect_ledger_chunks()` |
| 检索依赖交付本体之外：`ledger_files()` 将 `~/.workbuddy/MEMORY.md` 作为 curated ledger 源，`D:\AI-os\.workbuddy\memory\MEMORY.md` 不存在；README 明示为 v1 继承记忆源（设计决定，提示 Human 知悉外部依赖） | Info | 检索依赖 | —（设计记录） | `tools/experience_push.py` `ledger_files()` + `README.md` 启动顺序第 3 步 |
| `evolution/decisions.md` 缺 D-002：编号序列 D-001 → D-003，全库（不含 workspaces）无任何 D-002 引用或记录 | Low | 账本完整性 | 记录纪律（Human 裁决：补记或显式声明跳过） | `decisions.md` 全文 + 全库 grep "D-002" 零命中 |
| `decisions.md` 追加顺序与编号错位：D-013 排在 D-014/D-015 之后（文件顺序 …D-012、D-014、D-015、D-013），但 D-014 引用"D-013 真实运行"——按编号顺序阅读会先遇被引用的后记条目 | Low | 账本顺序 | 记录纪律（后续 append 不变，编号/顺序错位系 D-013 后补所致） | `decisions.md` 章节物理顺序 |
| SUCCESS_LOG 健康：22 条 Entry 全部含 footprint（matched/read/referenced/influence），无空白足迹；证据锚点文件基本在场 | Info（Positive） | 账本 | — | 逐条扫描：22/22 含 `footprint =` |
| growth.md 与 decisions/INT 一致：8 个对象行均有 D-xx/Entry 引用，INT-001~005 均在磁盘 | Info（Positive） | 账本 | — | `evolution/growth.md` vs `decisions.md` + `workspace/INT-*.md` 存在性 |
| `MAP.md` 与磁盘不一致：workspaces 行只列"entrychain / linkvault / devjournal / ai-consultant"，磁盘另有 a16_condA/B、a16lite_A/B、batch01~04、book_bare、book_exp 等 12 个实验目录 + 5 个 08-20 顶层文件（BATCH_OBSERVATIONS.md 等）；根级 `experiences/` 空目录未入图——违反"任何结构变化 → 同步更新本图" | Medium | MAP | 文档同步（无现成 maintenance Skill）→ Human 裁决 | `MAP.md` workspaces 行 vs 磁盘目录列表 + `git status`（?? workspaces/*） |
| git 安全网落后磁盘 3 天：HEAD 停在 2026-08-20（9d35fd1），工作区 12 个文件 M + star_map 2 个 D + ~45 项 untracked（含 capabilities/、evolution/、maintenance-skills/、task_cards 项目文件夹、全部 workspaces 项目、evidence/sessions/、4 个新 pattern）——"Git 是安全网"当前无法提供回滚保护 | High | Git | Human 裁决提交节奏（本次体检不提交、不 push） | `git status --porcelain` + `git log -1` |
| 无根 `.gitignore`，构建/数据产物入树：`tools/__pycache__/`（2 pyc）、各 workspaces 项目 `__pycache__`、`.smoke/notes.db`、`data/*.db` 等未跟踪 | Low | 仓库卫生 | Human 裁决（加 .gitignore） | 文件列表 + `git status`（?? tools/__pycache__/ 等） |
| `tools/README.md` 状态过期："⏳ 里程碑 2：在本目录实跑"仍标待接线验证，但 SUCCESS_LOG #1–#22 已证实 experience_push/wrapup_sync/task_card 长期实跑 | Low | 文档 | 文档同步 | `tools/README.md` vs `AI-OS_SUCCESS_LOG.md` |

## 诚实声明（无证据 = 没有）

- capabilities 覆盖重叠 / 触发冲突：**未发现**（唯一单位，互补关系显式声明）。
- domains 全库 rule_id 重复：**未发现**（92 篇 rule_id 唯一）。
- domains 全库 UTF-8 硬性解码失败：**未发现**（全部可解码；乱码系已固化的错位内容，非编码层损坏）。
- SUCCESS_LOG 足迹空白：**未发现**（22/22 含 footprint）。
- D-002 之外的 decisions 编号缺口：**未发现**（D-001~D-015 除 D-002 外均在册）。

## 已知待 Human 裁决项（不重复列为新问题）

- A18 experience_push reshape：R1（alias 入打分）已实现；R2（任务卡入索引）/ R3（意图源替代死钩子）等大改方向按 D-008 暂缓，由 task_start 统一发现部分承接。
- maintenance-skills 家族：capability-maintenance / retrieval-maintenance / task-memory-maintenance 按 D-014 暂不创建，本报告相关 findings 的"Suggested Maintenance Skill"以 knowledge-maintenance 为最近可用项或留空待裁决。

## 收尾说明

- 本次体检**只写入本报告**；未修改任何基础设施文件，未建/未归档任务卡，未 git commit / push。
- 输出即维护需求清单，交 Human 裁决（修 / 不修 / 以后修 / 忽略）。
